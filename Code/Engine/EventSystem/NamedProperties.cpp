#include "Engine/EventSystem/NamedProperties.hpp"

size_t NamedProperties::GetByteSizeOfAllProperties() {

	std::map<String, NamedBaseProperty*>::const_iterator mapIt = m_properties.begin();

	for (mapIt = m_properties.begin(); mapIt != m_properties.end(); ++mapIt) {


	}
}